/// Keep in sync with pubspec.yaml's `version:` field — no package_info
/// dependency needed just to show this on a settings screen.
const String appVersion = '0.1.0';

/// TODO: replace with the client's real support contact once decided
/// (proposal Section 7 — client provides support contact details).
const String supportEmail = 'support@vtuapp.example';
