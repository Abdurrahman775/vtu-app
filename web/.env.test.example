# Separate DB from .env's dev database so tests can freely truncate
# tables without touching data you're manually poking at in dev.
DATABASE_URL="postgresql://vtu_app:vtu_app_dev_password@localhost:5432/vtu_app_test?schema=public"
JWT_SECRET="test-secret"
